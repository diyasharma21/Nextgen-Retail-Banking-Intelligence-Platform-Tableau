USE rbip;

SHOW TABLES;

SELECT *
FROM customers
LIMIT 10;

SELECT *
FROM accounts
LIMIT 10;

SELECT *
FROM cards
LIMIT 10;

SELECT *
FROM loans
LIMIT 10;

SELECT *
FROM merchants
LIMIT 10;

SELECT *
FROM branches
LIMIT 10;

SELECT *
FROM transactions
LIMIT 10;